JOIN using derived columns (CTE / subquery)

Even if tables don’t align directly, you can create alignment.

WITH cte_emp AS (
  SELECT emp_id, SUBSTRING(code,1,3) AS dept
  FROM employees
)
SELECT *
FROM cte_emp e
JOIN departments d
ON e.dept = d.short_code;